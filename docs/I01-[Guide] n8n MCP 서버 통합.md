

# Claude Code + n8n MCP 서버 통합 완벽 가이드

> **AI 프롬프트만으로 n8n 워크플로우를 자동 생성·수정하는 강력한 시스템 구축하기**

---

## 📋 개요

Claude Code와 n8n MCP(Model Context Protocol) 서버를 결합하면, 복잡한 코딩이나 수동 작업 없이 **프롬프트만으로 n8n 워크플로우와 AI 에이전트를 직접 생성하고 수정**할 수 있습니다.

### 비유로 이해하기

이 시스템은 마치 **숙련된 건축가(Claude)**에게 **최신식 자동 건설 로봇(n8n MCP 서버)**과 **상세한 설계 매뉴얼(n8n Skills)**을 쥐여주는 것과 같습니다. 건축가는 당신의 말 한마디만 듣고도 설계도를 그리는 동시에 현장에서 로봇을 조종해 즉시 건물을 지어 올릴 수 있게 됩니다.

### n8n-MCP가 제공하는 것

| 항목 | 수량 | 설명 |
|------|------|------|
| n8n 노드 | 1,084개 | 537 코어 + 547 커뮤니티 노드 |
| 노드 속성 | 99% 커버리지 | 상세 스키마 포함 |
| 노드 작업 | 63.6% 커버리지 | 사용 가능한 액션 |
| 문서화 | 87% 커버리지 | 공식 n8n 문서 기반 |
| AI 도구 | 265개 | AI 기능 도구 변형 |
| 실제 예제 | 2,646개 | 인기 템플릿에서 추출한 구성 |

---

## 🔧 사전 준비 사항

### 필수 요구사항

1. **Node.js 18 이상**
   ```bash
   # 버전 확인
   node --version
   # v18.0.0 이상이어야 함
   ```
   
   설치가 안 되어 있다면: [nodejs.org](https://nodejs.org)에서 다운로드

2. **Claude Code CLI**
   ```bash
   # 설치 확인
   claude --version
   
   # 설치되지 않은 경우
   npm install -g @anthropic-ai/claude-code
   
   # 인증
   claude
   ```

1. **n8n 인스턴스**
   - n8n Cloud 또는 Self-hosted n8n
   - 로컬 Docker: `http://localhost:5678`
   - 클라우드: `https://your-instance.app.n8n.cloud`

---

## 📝 1단계: n8n API 키 발급

n8n에서 API 키를 발급받아야 Claude Code가 워크플로우를 직접 생성·관리할 수 있습니다.

### n8n Cloud 사용자

1. n8n 인스턴스에 로그인
2. 좌측 메뉴에서 **Settings** 클릭
3. **n8n API** 메뉴로 이동
4. **Create an API key** 버튼 클릭
5. Label(라벨)과 만료 기간 설정
6. 생성된 API 키를 **안전한 곳에 복사 저장**

> ⚠️ **중요**: API 키는 생성 시 한 번만 표시됩니다. 반드시 복사해두세요!

### Self-hosted n8n 사용자

1. n8n 대시보드 접속 (예: `http://localhost:5678`)
2. **Settings** → **n8n API** 이동
3. **Create an API key** 클릭
4. 생성된 키 복사

### API 키 보안 팁

- API 키는 비밀번호처럼 취급
- Git 저장소에 절대 커밋하지 않기
- 환경 변수로 관리 권장
- 주기적으로 키 갱신

---

## 🔌 2단계: n8n MCP 서버 구성

### 방법 A: Claude Code CLI로 직접 추가 (권장)

터미널에서 다음 명령어 실행:

```bash
claude mcp add n8n-mcp \
  --command "npx" \
  --args "n8n-mcp" \
  --env "MCP_MODE=stdio" \
  --env "LOG_LEVEL=error" \
  --env "DISABLE_CONSOLE_OUTPUT=true" \
  --env "N8N_API_URL=https://your-n8n-instance.com" \
  --env "N8N_API_KEY=your-api-key-here"
```

### 방법 B: mcp.json 파일 수동 생성

프로젝트 루트 또는 `~/.claude/` 디렉토리에 `mcp.json` 파일 생성:

```json
{
  "mcpServers": {
    "n8n-mcp": {
      "command": "npx",
      "args": ["n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "LOG_LEVEL": "error",
        "DISABLE_CONSOLE_OUTPUT": "true",
        "N8N_API_URL": "https://your-n8n-instance.com",
        "N8N_API_KEY": "your-api-key-here"
      }
    }
  }
}
```

### 운영체제별 설정 차이

| 운영체제 | 설정 |
|----------|------|
| **macOS / Linux** | 기본 설정 그대로 사용 |
| **Windows (WSL)** | 기본 설정 그대로 사용 |
| **Windows (일반, WSL 미사용)** | args를 `["cmd", "/c", "npx", "n8n-mcp"]`로 변경 |

#### Windows(WSL 미사용) 전용 설정

```json
{
  "mcpServers": {
    "n8n-mcp": {
      "command": "cmd",
      "args": ["/c", "npx", "n8n-mcp"],
      "env": {
        "MCP_MODE": "stdio",
        "LOG_LEVEL": "error",
        "DISABLE_CONSOLE_OUTPUT": "true",
        "N8N_API_URL": "https://your-n8n-instance.com",
        "N8N_API_KEY": "your-api-key-here"
      }
    }
  }
}
```

### 주의사항

- **N8N_API_URL**: 끝에 슬래시(/) 제거 필요!
  - ✅ 올바름: `https://your-instance.app.n8n.cloud`
  - ❌ 틀림: `https://your-instance.app.n8n.cloud/`
- 로컬 n8n은 `http://localhost:5678` 사용

### Scope 설정

| Scope | 설명 | 사용 사례 |
|-------|------|----------|
| `--scope local` (기본값) | 전역 사용자 설정에 저장 | 개인 프로젝트, API 키 비공개 유지 |
| `--scope project` | 프로젝트 `.mcp.json`에 저장 | 팀 공유, 프로젝트별 설정 |

```bash
# 전역(기본)
claude mcp add n8n-mcp --scope local ...

# 프로젝트 범위
claude mcp add n8n-mcp --scope project ...
```

---

## ✅ 3단계: 연결 확인

### MCP 서버 상태 확인

Claude Code를 실행하고 `/mcp` 명령어 입력:

```bash
claude
# Claude Code 실행 후
/mcp
```

**예상 결과**: `n8n-mcp: connected` 상태로 목록에 표시

### 사용 가능한 도구 확인

Claude에게 물어보기:

```
"n8n MCP에서 사용 가능한 도구들을 알려줘"
```

**주요 도구 목록**:

| 도구 | 설명 |
|------|------|
| `search_nodes` | 노드 검색 |
| `get_node` | 노드 상세 정보 |
| `validate_node` | 노드 구성 검증 |
| `search_templates` | 워크플로우 템플릿 검색 |
| `get_template` | 템플릿 상세 정보 |
| `n8n_create_workflow` | 워크플로우 생성 |
| `n8n_get_workflow` | 워크플로우 조회 |
| `n8n_update_partial_workflow` | 워크플로우 부분 수정 |

---

## 🎯 4단계: n8n Skills 설치 (선택사항, 권장)

n8n Skills는 Claude Code가 MCP 서버를 더 효율적으로 사용하도록 돕는 **맞춤 지침서**입니다.

### 제공되는 7가지 스킬

| 스킬 | 설명 | 활성화 시점 |
|------|------|------------|
| **n8n Expression Syntax** | 올바른 `{{}}` 패턴과 흔한 실수 | 표현식 작성, 변수 접근 시 |
| **n8n MCP Tools Expert** | MCP 도구 효과적 사용법 | 노드 검색, 검증, 템플릿 접근 시 |
| **n8n Workflow Patterns** | 5가지 검증된 아키텍처 패턴 | 워크플로우 생성, 노드 연결 시 |
| **n8n Validation Expert** | 검증 오류 해석 및 수정 가이드 | 검증 실패 시 |
| **n8n Node Configuration** | 작업 인식 설정 가이드 | 노드 구성 시 |
| **n8n Code JavaScript** | Code 노드에서 효과적인 JS 작성 | JavaScript 코드 작성 시 |
| **n8n Code Python** | Python 제한사항 인식 패턴 | Python 코드 작성 시 |

### 설치 방법 1: 플러그인 설치 (권장)

Claude Code에서 직접:

```
/plugin install czlonkowski/n8n-skills
```

### 설치 방법 2: 마켓플레이스 경유

```bash
# 마켓플레이스 추가
/plugin marketplace add czlonkowski/n8n-skills

# 플러그인 목록에서 설치
/plugin install
# "n8n-mcp-skills" 선택
```

### 설치 방법 3: 수동 설치

```bash
# 1. 저장소 클론
git clone https://github.com/czlonkowski/n8n-skills.git

# 2. 스킬 디렉토리로 복사
# macOS/Linux
mkdir -p ~/.claude/skills
cp -r n8n-skills/skills/* ~/.claude/skills/

# Windows
New-Item -ItemType Directory -Force -Path "$env:USERPROFILE\.claude\skills"
Copy-Item -Recurse n8n-skills\skills\* "$env:USERPROFILE\.claude\skills\"

# 3. Claude Code 재시작
```

### 전역(Global) 설정

설치된 스킬을 전역으로 설정하면 **모든 프로젝트에서** n8n 자동화 도구를 사용할 수 있습니다.

---

## 🚀 5단계: 실전 워크플로우 생성

### 프롬프트 작성 팁

**구체적으로 요청하세요:**

```
"텔레그램으로 식사 내용을 보내면 영양소를 분석해 
Supabase 데이터베이스에 저장하는 워크플로우를 만들어줘"
```

**다음 정보를 포함하면 더 좋습니다:**
- 트리거 유형 (Webhook, Schedule 등)
- 입력 데이터 형식
- 처리 로직
- 출력 대상

### 예제 1: Webhook → Slack 알림

```
다음 워크플로우를 만들어줘:
1. Webhook POST 요청으로 트리거
2. 요청 body에서 "email"과 "message" 필드 추출
3. #notifications 채널에 Slack 메시지 전송
4. Webhook에 성공 응답 반환

배포 전에 runtime 검증 프로파일로 검증해줘.
```

### 예제 2: 일정 기반 데이터 처리

```
매일 오전 9시에 실행되는 워크플로우:
1. Google Sheets에서 신규 데이터 가져오기
2. OpenAI로 데이터 요약
3. 요약 결과를 Notion 데이터베이스에 저장
4. 결과를 이메일로 발송
```

### 예제 3: AI 에이전트 워크플로우

```
LangChain AI 에이전트 워크플로우:
1. Chat 트리거로 사용자 입력 받기
2. GPT-4로 의도 분석
3. 도구(검색, 계산기 등) 자동 선택 및 실행
4. 결과 종합하여 응답
```

---

## 🔧 6단계: 워크플로우 수정 및 오류 해결

### 기존 워크플로우 수정

Claude는 기존 워크플로우도 수정할 수 있습니다:

```
"워크플로우 ID 12345의 Slack 노드를 Discord로 변경해줘"
```

내부적으로 `n8n_update_partial_workflow`를 사용해 **전체를 재생성하지 않고 해당 노드만 수정**합니다.

### 오류 발생 시

실행 중 오류가 발생하면 **오류 메시지를 그대로** Claude에게 전달:

```
"다음 오류가 발생했어:
[오류 메시지 붙여넣기]

워크플로우를 수정해줘."
```

Claude가 워크플로우의 전체 컨텍스트를 이해하고 있으므로 실시간으로 노드를 편집하여 문제를 해결합니다.

### 일반적인 문제 해결

| 문제 | 해결 방법 |
|------|----------|
| MCP 서버 연결 실패 | Node.js 설치 확인, `npx n8n-mcp` 직접 실행해 오류 확인 |
| 잘못된 JSON/깨진 노드 | "워크플로우 임포트가 실패했어. 올바른 문법으로 다시 생성해줘" |
| Rate limit 초과 | 60초 대기 후 재시도 |
| API 키 오류 | 키 복사 정확성 확인, 만료 여부 확인 |

---

## 📥 7단계: 워크플로우 가져오기 (API 키 미설정 시)

API 키 없이 사용하는 경우, Claude는 JSON을 생성하고 수동으로 가져와야 합니다:

1. Claude가 생성한 **JSON 코드 복사**
2. n8n에서 **우측 상단 메뉴(⋮)** 클릭
3. **Import from File** 또는 **Import from URL/JSON** 선택
4. JSON 붙여넣기
5. **Credentials 설정** (Slack, Gmail 등 각 서비스 인증)
6. 테스트 후 **활성화**

---

## 🛡️ 보안 고려사항

### API 키 보호

- n8n API 키는 **로컬에서만** 사용됨 (Anthropic으로 전송 안 됨)
- MCP 서버는 로컬 머신에서 실행
- 환경 변수로 관리 권장

### 베스트 프랙티스

```bash
# .env 파일 사용
N8N_API_URL=https://your-instance.com
N8N_API_KEY=your-key-here

# .gitignore에 추가
echo ".env" >> .gitignore
echo "mcp.json" >> .gitignore
```

---

## 📚 추가 리소스

### 공식 저장소

- **n8n-mcp**: [github.com/czlonkowski/n8n-mcp](https://github.com/czlonkowski/n8n-mcp)
- **n8n-skills**: [github.com/czlonkowski/n8n-skills](https://github.com/czlonkowski/n8n-skills)

### 문서

- [n8n 공식 문서](https://docs.n8n.io)
- [n8n API 인증](https://docs.n8n.io/api/authentication/)
- [Claude MCP 문서](https://docs.anthropic.com)

### 커뮤니티

- [n8n Community Forum](https://community.n8n.io)
- [n8n-mcp Discussions](https://github.com/czlonkowski/n8n-mcp/discussions)

---

## 🎯 요약 체크리스트

- [ ] Node.js 18+ 설치 확인
- [ ] Claude Code CLI 설치 및 인증
- [ ] n8n 인스턴스 준비
- [ ] n8n API 키 발급 (Settings → n8n API)
- [ ] mcp.json 설정 또는 `claude mcp add` 실행
- [ ] `/mcp` 명령으로 연결 확인
- [ ] (선택) n8n-skills 플러그인 설치
- [ ] 첫 워크플로우 생성 테스트!

---

*이 가이드는 2025년 1월 기준 최신 정보를 바탕으로 작성되었습니다.*